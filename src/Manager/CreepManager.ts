import WorldManager from 'Manager/WorldManager';
import Room from 'Model/Room';

export default class CreepManager {
    public constructor(private worldManager: WorldManager) {
    }

    public process(room: Room): void {

    }
}
